x = 3.14
y = 4
z = 5

# round() = arredonda o número em questão
# resultado = round(x)

# abs() = É o valor absoluto de um número, ou seja a distância dele do 0
# resultado = abs(y)

# pow() = elevado, precisa-se de um número para ser elevado, exemp = pow(4, 3)
# resultado = pow(4, 3)

# max() = acha o maior valor entre vários números
# resultado = max(x, y, z)
# min() = acha o menor valor entre vários números
# resultado = min(x, y, z)

print(resultado)